{
  "name": "karaoke",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rimraf dist && parcel build './src/**/*.html' --no-cache --public-url /karaoke",
    "start": "rimraf dist && parcel serve './src/**/*.html' --no-cache --open",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.11.0",
    "rimraf": "^5.0.5"
  }
}